import React from "react";

const Remaining = () => {
    return (
        <div>
            <span className="alert alert-success">
                Remaining: $1000
            </span>
        </div>
    );
}

export default Remaining;